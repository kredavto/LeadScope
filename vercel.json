{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @leadscope/web build",
  "outputDirectory": "apps/web/.next",
  "env": {
    "NEXT_PUBLIC_DEMO_MODE": "true"
  }
}
